What is this?

This is a Telegram bot, that can do what you ask it to. It connects to a LLM to interpret your requests. Then uses Model Context Protocol (MCP) servers to actually execute the request.
